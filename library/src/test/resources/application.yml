spring:
  data:
    mongodb:
      port: 0
  shell:
    interactive:
      enabled: false
mongock:
  runner-type: "InitializingBean"
  change-logs-scan-package:
    - ru.otus.library.mongock.test.changelog